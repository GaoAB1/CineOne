# 2026-08-25 工作日志

## CineOne 影视聚合应用（标准 SOP 全流程）

- **团队协作**：software-cineone 团队，许清楚 PRD → 高见远架构 → 寇豆码实现 → 严过关 QA
- **文档**：docs/PRD.md（P0-P2 需求池）、docs/ARCHITECTURE.md（Express+better-sqlite3 / Vite+React+TS+Tailwind，17 API，5 表 DDL，Apple 设计令牌）
- **已拍板决策**：豆瓣/烂番茄/爆米花=第三方聚合+SQLite缓存+手动兜底；TMDB Key 用户自配存 settings；追剧季/集二级；MVP 单管理员；经典分区=top_rated 且年份≥10年；JWT_SECRET 环境变量优先否则生成持久化到 data/.jwt_secret
- **实现**：约 80 个源文件落盘；修复 6 个问题（ratingsService 类型 2 处、tmdbService crew 取值+类型注解、postcss.config.js→.cjs）
- **验证通过**：双端 tsc、vite build（需 --emptyOutDir=false 绕过 safe-delete shim）、bootstrap/setup/login/me/watchlist 增删改查/评分降级全链路冒烟
- **GitHub**：仓库 GaoAB1/CineOne（public）。**重要发现：HTTPS + x-access-token:PAT 方式 git push 可用**（旧记忆"git push 被墙不可用"不成立，至少对 HTTPS token 方式）；token 从 ~/.workbuddy/mcp.json 的 github-pat env 读取，勿打印
- **CI**：.github/workflows/docker-image.yml，push main 自动构建镜像推 GHCR（latest + sha 标签），首次运行触发于 commit 0a889c3

## 本机环境备忘

- 端口 3000 被 MovieHome 服务长期占用，本项目冒烟/开发用 PORT=3456
- safe-delete shim 会拦截 rm 和 vite 的 emptyOutDir：前端构建统一加 `--emptyOutDir=false`
