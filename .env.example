# CineOne 环境变量示例
# 复制为 .env 或在 docker-compose / docker run 中注入

# 服务监听端口（默认 3000）
PORT=3000

# JWT 签名密钥：生产环境务必显式配置
# 缺省时首次启动会自动生成随机值并持久化到数据目录（server/data/.jwt_secret），重启不失效
# JWT_SECRET=change-me-to-a-long-random-string
